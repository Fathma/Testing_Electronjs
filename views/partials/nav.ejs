
<div class="wrapper">
    <!-- Sidebar -->
    <nav id="sidebar">
        <div class="sidebar-header">
            <h3>Bootstrap Sidebar</h3>
        </div>
        <ul class="list-unstyled components">
            <li>
                <a href="#" onclick="loadMain()">Home</a>
            </li>
            <li>
                <a href="#" onclick="loadReg()">Registration</a>
            </li>
            <li>
                <input type="text" id="key" placeholder="Search" name="key"  value="<%if (data.key) { %><%= data.key %><% } %>"  ><button onclick="search()">Search</button>
            </li>
            <li>
                <select id="lan">
                    <option value="en" <% if(la == "en") { %> selected  <% } %> >en</option>
                    <option value="sv" <% if(la == "sv") { %> selected  <% } %> >sv</option>
                    <option value="bn" <% if(la == "bn") { %> selected  <% } %> >bn</option>
                </select> 
            </li>
          
        </ul>
    </nav>
    <!-- Page Content -->
    <div id="content">

        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                <button type="button" id="sidebarCollapse" class="btn btn-info">
                    <i class="fas fa-align-left"></i>
                    <span>Toggle Sidebar</span>
                </button>
            </div>
        </nav>

    </div>
</div>  
